import React from 'react'

import classes from './MainSpinner.css'

const spinner = () => {
    return (
        <div className={classes.loader}>
        
        </div>
    );
};

export default spinner;